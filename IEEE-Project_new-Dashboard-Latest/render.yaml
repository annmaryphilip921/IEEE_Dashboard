services:
  - type: web
    name: ieee-dashboard
    runtime: node
    rootDir: IEEE-Project_new-Dashboard-Latest
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001